#include"New.h"
int main() {
	player now;
	now.score = -1;
	int temp;
	firstshow(now);
	if (bint(temp) == 0)  return 0;
	system("cls");
	if(now.score==-1)  	Register(now);
	mainshow(now);              //������
	return 0;
}